# ----------------------------------------------------------------------
# PATH & 各种工具初始化 (优化版 - 延迟加载)
# ----------------------------------------------------------------------

# 固定路径（不需要延迟加载的）

export PATH="$HOME/.local/bin:$PATH"
